---
paths:
  - "app/policy/**"
  - "data/policies/**"
---

# 타이틀·소제목 — 사장님이 준다 (2026-09-19 확정)

1. 타이틀과 소제목 4개는 사장님이 spec 으로 준다 (`scripts/specs/{허브}.md`). 기계도 나도 짓지 않는다.
2. **글자 하나 바꾸지 않는다.** 후킹·패턴·캡처·실검색어 조립·판정 — 전부 없다 (2026-09-19 삭제).
3. **바꾸자고 제안하지 않는다.** 어색해 보여도 그대로 쓴다. 문제가 있으면 보고서 메모에 한 줄 적고 그대로 쓴다.
4. 소제목 = qa 카드 4개 (spec 순서 그대로), FAQ 2개. 버튼 슬롯은 qa 인덱스 2·4·마지막 (`SpokeClient.tsx`) — 스크립트가 잡는다.
5. 버튼 목적지는 Playwright 로 먼저 열어 확인한 뒤 문구를 쓴다 (`action-copy.md`).
